# 精简 MCP 工具声明文本

## Meta

- status: done
- source: user-prompt

## 用户原始描述

降低 MCP 工具声明本身的上下文成本。要求：

- 精简 register-read-tools 和 register-write-tools 中的 tool description，保留用途和关键护栏，不重复长流程说明。
- 精简 write-schemas 中字段 describe 文案，避免每个字段都塞长解释。
- 保留 spec_context before writes 的强护栏语义。
- 不改变工具名称、参数结构、运行时行为和现有返回内容。
- 更新 smoke/unit 测试中工具 description 断言，验证关键短语仍存在。
- 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## TODO

- [X]  精简 register-read-tools 和 register-write-tools 中的 tool description，保留用途和关键护栏，不重复长流程说明。
- [X]  精简 write-schemas 中字段 describe 文案，避免每个字段都塞长解释。
- [X]  保留 spec_context before writes 的强护栏语义。
- [X]  不改变工具名称、参数结构、运行时行为和现有返回内容。
- [X]  更新 smoke/unit 测试中工具 description 断言，验证关键短语仍存在。
- [X]  验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## Done

- doneAt: 2026-06-20T18:11:40.500Z
- note: 精简 MCP 工具声明文本，降低工具 schema/description 上下文成本。

## 最终行为契约


| 场景                     | 条件                                           | 结果                                                                                          | 默认行为                                                     | 边界处理                                                                                   | 验证                                                                                        | 关联文件                                                                                 |
| ------------------------ | ---------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| MCP 工具声明精简         | 模型加载 Spec Coding MCP 工具定义时            | read/write 工具 description 保留入口定位、用途和 spec_context 护栏短语，不再重复长流程说明。  | 工具名称、参数结构、运行时返回内容保持不变；只减少声明文本。 | 写工具 description 仍包含 Requires prior spec_context，运行时 guard 仍返回原有强错误信息。 | bun run build; bun test/unit.ts; bun test/smoke.ts; bun run release:check; git diff --check | `src/mcp/register-read-tools.ts`<br>`src/mcp/register-write-tools.ts`<br>`test/smoke.ts` |
| 写入 schema 字段说明精简 | 模型读取 checkpoint/review/done 参数 schema 时 | behaviorRecords、file、summary、completedTodos 等字段说明改为短描述，降低工具声明上下文成本。 | 字段名、类型、默认值和校验规则不变。                         | 最终行为记录仍通过 behaviorRecords 结构化传入，空记录时 writer 仍按既有逻辑提示补充。      | bun run build; bun test/unit.ts; bun test/smoke.ts                                          | `src/mcp/write-schemas.ts`<br>`test/smoke.ts`                                            |
