# 行为记录改为文字描述

## Meta

- status: done
- source: user-prompt

## 用户原始描述

将 behaviorRecords 的 Markdown 渲染从表格改为更适合人类阅读的分组文字描述。

要求：
- 不再使用 Markdown 表格渲染实际行为记录和最终行为契约。
- 每条行为记录用编号列表展示，字段用子 bullet 描述：条件、结果、默认行为、边界处理、验证、关联文件。
- 输出风格接近用户给出的示例：一眼能看出完成了哪些业务行为、每个行为的分支和验证。
- 保留缺少 behaviorRecords 时“不可作为真实行为事实”的强提示。
- 更新 unit/smoke 测试断言。
- 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、git diff --check。

## TODO

- [x] 不再使用 Markdown 表格渲染实际行为记录和最终行为契约。
- [x] 每条行为记录用编号列表展示，字段用子 bullet 描述：条件、结果、默认行为、边界处理、验证、关联文件。
- [x] 输出风格接近用户给出的示例：一眼能看出完成了哪些业务行为、每个行为的分支和验证。
- [x] 保留缺少 behaviorRecords 时“不可作为真实行为事实”的强提示。
- [x] 更新 unit/smoke 测试断言。
- [x] 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、git diff --check。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## Done

- doneAt: 2026-06-20T18:34:16.613Z
- note: 行为记录改为编号文字描述，不再使用表格。

## 最终行为契约

1. 行为记录文字化
  - 条件：checkpoint、review result 或 done writer 调用 behaviorRecordLines 渲染 behaviorRecords 时
  - 结果：输出编号列表，每条记录下面按条件、结果、默认行为、边界处理、验证、关联文件分组展示，不再输出 Markdown 表格。
  - 默认行为：behaviorRecords 入参结构保持不变；没有关联文件时显示“关联文件：未记录”。
  - 边界处理：behaviorRecords 为空时仍输出“未提供已验证行为”和“不可作为真实行为事实”的强提示。
  - 验证：bun run build; bun test/unit.ts; bun test/smoke.ts; git diff --check
  - 关联文件：
    - `src/spec/behavior-record.ts`
    - `test/unit.ts`
    - `test/smoke.ts`
