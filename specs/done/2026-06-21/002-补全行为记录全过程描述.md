# 补全行为记录全过程描述

## Meta

- status: done
- source: user-prompt

## 用户原始描述

- [x]  定位行为记录模板、渲染和测试覆盖
- [x]  将实际行为记录从摘要字段改为功能全过程描述指引
- [x]  更新测试或快照，验证 checkpoint、done、review 相关输出
- [x]  记录 checkpoint 并归档任务

## TODO

- [x]  定位行为记录模板、渲染和测试覆盖
- [x]  将实际行为记录从摘要字段改为功能全过程描述指引
- [x]  更新测试或快照，验证 checkpoint、done、review 相关输出
- [x]  记录 checkpoint 并归档任务

## 实际行为记录

- 记录来源：只能记录已阅读代码、已修改代码、测试结果或用户确认的事实。
- 分支条件：完成后补充实际存在的正常、失败、边界、权限和状态分支。
- 默认参数行为：完成后补充源码里的默认值、配置来源和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令、结果和关联文件。
- 禁止事项：不要把猜测、常识或“看起来合理”的行为写成事实。

## Done

- doneAt: 2026-06-21T10:44:14.835Z
- note: 按用户反馈补全行为记录为功能全过程描述：新增触发入口、输入与前置状态、执行过程、输出结果、副作用等结构化字段，并同步模板、README、测试和 specs 文档。

## 最终行为契约

1. 行为记录全过程描述
  - 条件：用户指出现有行为记录只像分支摘要，不足以描述无响应功能点的完整运行过程
  - 结果：行为记录输出升级为功能全过程结构，展示触发入口、输入与前置状态、执行过程、输出结果、副作用、默认行为、边界处理、结果摘要、验证和关联文件。
  - 默认行为：旧的 scenario、condition、result、defaultBehavior、edgeCase、verification、relatedFiles 字段继续兼容；未提供新字段时显示未记录占位。
  - 边界处理：当前 MCP 进程仍使用旧 schema，归档记录使用旧必填字段；源码 schema 和测试已覆盖新字段。
  - 验证：npm run build、bun test/unit.ts、bun test/smoke.ts、npm run release:check、git diff --check 均通过。
  - 关联文件：
    - `src/spec/behavior-record.ts`
    - `src/spec/types.ts`
    - `src/mcp/write-schemas.ts`
    - `src/templates/prompt-documents.ts`
    - `src/templates/spec-documents.ts`
    - `test/unit.ts`
    - `test/smoke.ts`
    - `README.md`
