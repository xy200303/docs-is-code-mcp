# 拆分 status 推荐决策模块

## Meta

- status: done
- source: user-prompt

## 用户原始描述

重构 `specc status` 的结构化推荐决策，把 `statusDecision`、`schemaVersion` 和相关类型从 `src/cli/command-status.ts` 拆到独立模块，降低 CLI 命令文件复杂度并方便单测。

目标：
- 新增独立模块承载 status JSON schemaVersion、WorkflowState、StatusRecommendation、StatusDecision 和决策函数。
- `command-status.ts` 只负责参数解析、读取 specs、渲染文本/JSON。
- 行为保持不变：empty、done-only、open TODO/active 的 nextStep 和 recommendation 不变。
- 新增或更新 unit 测试直接覆盖推荐决策函数。
- 保持现有 smoke 测试继续通过。

验收：
- `bun run build` 通过。
- `bun test/unit.ts` 通过。
- `bun test/smoke.ts` 通过。
- `bun run release:check` 通过。
- `git diff --check` 通过。

## TODO

- [x] 重构 `specc status` 的结构化推荐决策，把 `statusDecision`、`schemaVersion` 和相关类型从 `src/cli/command-status.ts` 拆到独立模块，降低 CLI 命令文件复杂度并方便单测。
- [x] 目标：
- [x] 新增独立模块承载 status JSON schemaVersion、WorkflowState、StatusRecommendation、StatusDecision 和决策函数。
- [x] `command-status.ts` 只负责参数解析、读取 specs、渲染文本/JSON。
- [x] 行为保持不变：empty、done-only、open TODO/active 的 nextStep 和 recommendation 不变。
- [x] 新增或更新 unit 测试直接覆盖推荐决策函数。
- [x] 保持现有 smoke 测试继续通过。
- [x] 验收：
- [x] `bun run build` 通过。
- [x] `bun test/unit.ts` 通过。
- [x] `bun test/smoke.ts` 通过。
- [x] `bun run release:check` 通过。
- [x] `git diff --check` 通过。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T16:25:25.774Z
- summary: 已将 specc status 的结构化推荐决策拆分到独立模块，CLI 命令文件保留参数解析、spec 读取和输出渲染职责，并补充推荐决策单测覆盖。

### Summary

- 已将 specc status 的结构化推荐决策拆分到独立模块，CLI 命令文件保留参数解析、spec 读取和输出渲染职责，并补充推荐决策单测覆盖。

### Completed TODOs

- 重构 `specc status` 的结构化推荐决策，把 `statusDecision`、`schemaVersion` 和相关类型从 `src/cli/command-status.ts` 拆到独立模块，降低 CLI 命令文件复杂度并方便单测。
- 目标：
- 新增独立模块承载 status JSON schemaVersion、WorkflowState、StatusRecommendation、StatusDecision 和决策函数。
- `command-status.ts` 只负责参数解析、读取 specs、渲染文本/JSON。
- 行为保持不变：empty、done-only、open TODO/active 的 nextStep 和 recommendation 不变。
- 新增或更新 unit 测试直接覆盖推荐决策函数。
- 保持现有 smoke 测试继续通过。
- 验收：
- `bun run build` 通过。
- `bun test/unit.ts` 通过。
- `bun test/smoke.ts` 通过。
- `bun run release:check` 通过。
- `git diff --check` 通过。

### Changed Files

- `src/cli/status-recommendation.ts`
- `src/cli/command-status.ts`
- `test/unit.ts`

### Verification

- passed `bun run build`：TypeScript 构建通过。
- passed `bun test/unit.ts`：单元测试通过，包含 status 推荐决策分支覆盖。
- passed `bun test/smoke.ts`：MCP smoke 测试通过。
- passed `bun run release:check`：发布契约检查通过。
- passed `git diff --check`：退出码为 0，仅出现 Windows LF/CRLF 提示。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| status 推荐决策归属 | 调用 specc status 或 specc status --json 时需要生成 nextStep 与 recommendation | schemaVersion、WorkflowState、StatusRecommendation、StatusDecision 和推荐分支由 src/cli/status-recommendation.ts 统一承载。 | 未记录 | 未记录 | bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`src/cli/command-status.ts` |
| CLI 命令职责 | runStatus 处理用户传入参数并输出文本或 JSON | command-status.ts 只负责参数解析、读取 specs、统计 open TODO、构建报告和渲染输出。 | 未记录 | 未记录 | bun run build && bun test/smoke.ts | `src/cli/command-status.ts` |
| 推荐分支保持不变 | empty、done-only、active/review/todo、open TODO 四类工作流状态 | empty 推荐 spec_bootstrap；done-only 推荐 spec_todo；存在 active/todo/review 推荐 spec_context；存在 open TODO 时 nextStep 明确要求按 TODO 顺序执行。 | 未记录 | openTodos 大于 0 时优先级高于 active/todo/review 普通推荐。 | bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`test/unit.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T16:25:37.601Z
- note: status 推荐决策拆分完成并通过全部验收。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| status 推荐决策归属 | 调用 specc status 或 specc status --json 时需要生成 nextStep 与 recommendation | schemaVersion、WorkflowState、StatusRecommendation、StatusDecision 和推荐分支由 src/cli/status-recommendation.ts 统一承载。 | 未记录 | 未记录 | bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`src/cli/command-status.ts` |
| CLI 命令职责 | runStatus 处理用户传入参数并输出文本或 JSON | command-status.ts 只负责参数解析、读取 specs、统计 open TODO、构建报告和渲染输出。 | 未记录 | 未记录 | bun run build && bun test/smoke.ts | `src/cli/command-status.ts` |
| 推荐分支保持不变 | empty、done-only、active/review/todo、open TODO 四类工作流状态 | empty 推荐 spec_bootstrap；done-only 推荐 spec_todo；存在 active/todo/review 推荐 spec_context；存在 open TODO 时 nextStep 明确要求按 TODO 顺序执行。 | 未记录 | openTodos 大于 0 时优先级高于 active/todo/review 普通推荐。 | bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`test/unit.ts` |
