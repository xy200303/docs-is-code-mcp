# 修复 Windows 手动发布脚本

## Meta

- status: done
- source: user-prompt

## 用户原始描述

修复 scripts/release-manual.mjs 在 Windows 上执行 git commit -m "发布 <version>" 时提交信息参数被 shell 拆分的问题。要求：
- 不影响 Linux/macOS 行为。
- 优先使用 spawnSync 的参数数组语义，不让 shell 重新拆分 commit message。
- release:manual --publish 能正常提交版本 bump、创建 tag、推送 main 和 tag。
- release-check 增加契约检查，防止 git commit 继续依赖会拆分参数的 shell 模式。
- 更新 unit 或 release-check 测试；验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## TODO

- [x] 不影响 Linux/macOS 行为。
- [x] 优先使用 spawnSync 的参数数组语义，不让 shell 重新拆分 commit message。
- [x] release:manual --publish 能正常提交版本 bump、创建 tag、推送 main 和 tag。
- [x] release-check 增加契约检查，防止 git commit 继续依赖会拆分参数的 shell 模式。
- [x] 更新 unit 或 release-check 测试；验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## Done

- doneAt: 2026-06-20T17:53:33.958Z
- note: Windows release-manual 参数拆分问题已修复。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| Windows 手动发布提交 | 运行 npm run release:manual -- <version> --publish 到 git commit 步骤 | git commit 接收完整的 `发布 <version>` 作为单个 -m 参数，不再把版本号当 pathspec。 | spawnSync 保持参数数组语义；Windows、Linux、macOS 都使用同一执行方式。 | git ls-remote、npm view 等检查命令也不启用 shell，避免未来出现类似参数拆分问题。 | bun run release:check | `scripts/release-manual.mjs`<br>`scripts/release-check.mjs` |
| 发布契约检查 | 运行 bun run release:check | 检查 release-manual 包含禁用 shell 的说明，并拒绝 `shell: process.platform` 这类平台 shell 模式。 | 发布前静态检查阻止 Windows 参数拆分回归。 | 未记录 | bun run release:check | `scripts/release-check.mjs` |
