# 空任务上下文输出结构化下一步

## Meta

- status: done
- source: user-prompt

## 用户原始描述

优化 spec_context 在没有 active/todo/review selected spec 且没有 open TODO 时的输出：除了现有提示，还要渲染与 spec_list/status 一致的 Recommended Next Step 结构化区块，让模型明确下一步应调用 spec_bootstrap、spec_todo 或 spec_create。要求：
- 不改变已有 active/todo/review/done-only 推荐语义。
- 空项目推荐 spec_bootstrap；已有 done 但无当前任务推荐 spec_todo；如果未来存在 review-only 仍保持现有 review 处理。
- spec_context 的 Markdown 输出包含 nextTool、reason、when、afterwards、arguments 字段。
- 更新 smoke/unit 测试覆盖空上下文的结构化推荐字段。
- 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## TODO

- [x] 优化 spec_context 在没有 active/todo/review selected spec 且没有 open TODO 时的输出：除了现有提示，还要渲染与 spec_list/status 一致的 Recommended Next Step 结构化区块，让模型明确下一步应调用 spec_bootstrap、spec_todo 或 spec_create。要求：
- [x] 不改变已有 active/todo/review/done-only 推荐语义。
- [x] 空项目推荐 spec_bootstrap；已有 done 但无当前任务推荐 spec_todo；如果未来存在 review-only 仍保持现有 review 处理。
- [x] spec_context 的 Markdown 输出包含 nextTool、reason、when、afterwards、arguments 字段。
- [x] 更新 smoke/unit 测试覆盖空上下文的结构化推荐字段。
- [x] 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T17:12:07.766Z
- summary: 补强 spec_context 空任务和 done-only 场景的结构化 Recommended Next Step 回归测试，确保 nextTool、arguments、reason、when、afterwards 字段不会退化。

### Summary

- 补强 spec_context 空任务和 done-only 场景的结构化 Recommended Next Step 回归测试，确保 nextTool、arguments、reason、when、afterwards 字段不会退化。

### Completed TODOs

- 优化 spec_context 在没有 active/todo/review selected spec 且没有 open TODO 时的输出：除了现有提示，还要渲染与 spec_list/status 一致的 Recommended Next Step 结构化区块，让模型明确下一步应调用 spec_bootstrap、spec_todo 或 spec_create。要求：
- 不改变已有 active/todo/review/done-only 推荐语义。
- 空项目推荐 spec_bootstrap；已有 done 但无当前任务推荐 spec_todo；如果未来存在 review-only 仍保持现有 review 处理。
- spec_context 的 Markdown 输出包含 nextTool、reason、when、afterwards、arguments 字段。
- 更新 smoke/unit 测试覆盖空上下文的结构化推荐字段。
- 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

### Changed Files

- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript 构建通过。
- passed `bun test/unit.ts`：单元测试通过。
- passed `bun test/smoke.ts`：空项目和 done-only spec_context 推荐字段契约通过。
- passed `bun run release:check`：发布契约检查通过。
- passed `git diff --check`：退出码 0，仅 Windows LF/CRLF 提示。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 空项目 spec_context | 没有 active、todo、review、done、selected spec 或 open TODO | Markdown 输出 Recommended Next Step，nextTool 为 spec_bootstrap，并包含 alternatives、arguments、reason、when、afterwards。 | projectKind 使用 auto；用户已有明确小任务或功能需求时 alternatives 保留 spec_todo/spec_create。 | 不会直接引导实现代码，而是要求先建立项目入口。 | bun test/smoke.ts | `test/smoke.ts`<br>`src/spec/context-markdown.ts`<br>`src/spec/workflow-next-step.ts` |
| done-only spec_context | 项目只有 done specs，没有当前待执行任务 | Markdown 输出 Recommended Next Step，nextTool 为 spec_todo，并包含 prompt/title 占位参数、reason、when、afterwards。 | 不回退 spec_bootstrap，保持已有 done-only 推荐语义。 | 不编造新需求内容，prompt/title 保持占位等待用户或模型基于明确任务创建。 | bun test/smoke.ts | `test/smoke.ts`<br>`src/spec/workflow-next-step.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T17:12:19.888Z
- note: 结构化推荐字段契约已由 smoke 测试保护。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 空项目 spec_context | 没有 active、todo、review、done、selected spec 或 open TODO | Markdown 输出 Recommended Next Step，nextTool 为 spec_bootstrap，并包含 alternatives、arguments、reason、when、afterwards。 | projectKind 使用 auto；用户已有明确小任务或功能需求时 alternatives 保留 spec_todo/spec_create。 | 不会直接引导实现代码，而是要求先建立项目入口。 | bun test/smoke.ts | `test/smoke.ts`<br>`src/spec/context-markdown.ts`<br>`src/spec/workflow-next-step.ts` |
| done-only spec_context | 项目只有 done specs，没有当前待执行任务 | Markdown 输出 Recommended Next Step，nextTool 为 spec_todo，并包含 prompt/title 占位参数、reason、when、afterwards。 | 不回退 spec_bootstrap，保持已有 done-only 推荐语义。 | 不编造新需求内容，prompt/title 保持占位等待用户或模型基于明确任务创建。 | bun test/smoke.ts | `test/smoke.ts`<br>`src/spec/workflow-next-step.ts` |
